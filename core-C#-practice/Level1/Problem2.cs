using System;
public class Demo
{
    public static void Main()
    {
        int maths = 94;
        int phy = 95;
        int chem = 96;

        double percent = (maths + phy + chem)/3.0; 
        Console.WriteLine(" Sam’s average mark in PCM is: " + percent);
    }
}